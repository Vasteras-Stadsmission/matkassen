#!/bin/sh
set -e

pnpm run validate &
pid_validate=$!

pnpm test &
pid_test=$!

fail=""
wait $pid_validate || fail="$fail validate"
wait $pid_test || fail="$fail test"

if [ -n "$fail" ]; then
  echo ""
  echo "Pre-push checks failed:$fail"
  exit 1
fi
